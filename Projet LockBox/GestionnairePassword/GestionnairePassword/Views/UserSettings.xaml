<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionnairePassword.Views.UserSettings"
             Title="Paramètre">
    
    <VerticalStackLayout x:Name="Form" 
                         Spacing="25"
                         Padding="20">

        <Frame HeightRequest="45" Margin="-10,0,0,0" Padding="0" HasShadow="True" BorderColor="#337df4"  HorizontalOptions="FillAndExpand">
            <Entry x:Name="OldPassword" IsPassword="True" Placeholder="Ancien mot de passe" />
        </Frame>

        <Frame HeightRequest="45" Margin="-10,0,0,0" Padding="0" HasShadow="True" BorderColor="#337df4"  HorizontalOptions="FillAndExpand">
            <Entry x:Name="NewPassword" IsPassword="True" Placeholder="Nouveau mot de passe" />
        </Frame>

        <Button Text="Changer le mot de passe" Clicked="Change_Clicked" WidthRequest="200" CornerRadius="20" />
    </VerticalStackLayout>
</ContentPage>